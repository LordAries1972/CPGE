WarpDotTunnel circular sway, Y axis, look-ahead 5 — 16/05/2026 (fourth pass)

Init3DWarpDOTTunnel:
- ring.bornCy initialised alongside ring.bornCx at stagger time:
    bornCy = y + kSideWaveRadius * cosf(fraction * 2π)
  gives a circular (not diagonal) spread of initial birth positions.

UpdateWarpDotTunnel:
- Wrap block now assigns both bornCx AND bornCy:
    phase = sideWaveTime * kSideWaveSpeed
    bornCx = startX + 60 * sin(phase)
    bornCy = startY + 60 * cos(phase)
  The X and Y waves are 90° out of phase (sin vs cos), so the far end of the
  tunnel traces a circle rather than swaying along a single axis.
- ring.cy = ring.bornCy (was = data.startY; now moves with birth Y offset).
- Camera look-ahead index changed from min(9,...) to min(4,...) — 5th-nearest
  ring used as target, reducing the camera's tracking lag and smoothing jaggedness.
