// DX12RenderFrame.cpp -- change log 14-06-2026
// ZoomInOut FX render-order integration (two passes):
//
// Pass 1: IsImageZoomActive() guards skipping Blit2DObjectToSize when FX active.
//
// Pass 2: All guards converted to if/else — RenderZoomedImage() called with same
//         dest parameters when IsImageZoomActive() returns true.
//         Images affected (both main pass and RenderBackgroundImage pre-pass):
//           IMG_GAMEINTRO1, IMG_COMPANYLOGO, IMG_SPLASH1, IMG_LOADING (multiple paths).
