// DXRenderFrame.cpp -- change log 14-06-2026
// ZoomInOut FX render-order integration (two passes):
//
// Pass 1: IsImageZoomActive() guards added to each Blit2DObjectToSize call — skipping
//         the normal blit when the FX is active.
//
// Pass 2: All guards converted to if/else — when IsImageZoomActive() returns true,
//         fxManager.RenderZoomedImage() is called with the same dest parameters at
//         the exact render-order position where the normal blit would occur.
//         Images affected: IMG_SPLASH1, IMG_COMPANYLOGO (x2), IMG_GAMEINTRO1,
//                          IMG_LOADING (x2 scenes).
