// History file for GUIConfigWindow.cpp — 18-05-2026b
// Changes:
//   1. AVAILABLE_RENDERERS — Windows now always lists all 4 renderers (DX11, DX12, OpenGL, Vulkan)
//      unconditionally, regardless of compile-time backend defines. User can select any renderer
//      to set config.myConfig.rendererType; restart applies the new executable.
//      Non-Windows platforms retain compile-time filtering.
//
//   2. Near Plane (t0_near) — changed from read-only addInfoRow to addSliderRow.
//      Range: 0.1 => 2.0, 2 decimal places. Writes directly to config.myConfig.nearPlane.
//
//   3. Far Plane (t0_far) — changed from read-only addInfoRow to addSliderRow.
//      Range: 500 => 2000, integer display. Writes directly to config.myConfig.farPlane.
//
//   4. rendSliderStart calculation simplified — loop replaced with std::clamp since
//      on Windows AVAILABLE_RENDERERS[i].type == i always.
