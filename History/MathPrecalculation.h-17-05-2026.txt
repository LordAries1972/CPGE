MathPrecalculation.h — History 17-05-2026
DirectXMath include and namespace guarded for cross-platform builds.

Changes:
- Changed unconditional '#include "DirectXMath.h"' and 'using namespace DirectX;' to:
  #if defined(__USE_DIRECTX_11__) || defined(__USE_DIRECTX_12__)
      #include <DirectXMath.h>
      using namespace DirectX;
  #endif
- All struct fields using XMFLOAT2/3/4/XMMATRIX now resolve via Includes.h type aliases on non-DX builds.
- No structural or functional changes to precalculation methods or lookup tables.
