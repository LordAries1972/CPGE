DX_FXManager.h - 08-05-2026
============================
Changes for WarpDotTunnel FX system:

1. FXType enum: added WarpDotTunnel value.

2. TunnelSpinCycle enum added (after FXSubType):
   - None, Clockwise, AntiClockwise

3. TunnelRing struct added:
   - zPos, spinAngle, cx, cy, alive

4. WarpTunnelData struct added:
   - startX/Y/Z, minRadius, maxRadius, spinCycle, travelSpeed, reverseTravel,
     dotsPerCircle, density, totalDistance, nearZ, farZ, spinSpeed
   - static constexpr kMaxXYRadius = 300.0f
   - std::vector<TunnelRing> rings

5. FXItem struct: added warpTunnelData member (WarpTunnelData).

6. ActiveFXState struct: added tunnelActive (bool) and tunnelID (int) fields,
   mirrored across all constructors and assignment operators.

7. FXManager class:
   - Public: int tunnelID = 0;
   - Public: Init3DWarpDOTTunnel(x, y, z, minRadius, maxRadius, spinCycle,
                                  travelSpeed, reverseTravel, dotsPerCircle, density)
   - Public: StopWarpDotTunnel()
   - Private: UpdateWarpDotTunnel(FXItem& fx, float deltaTime)
   - Private: RenderWarpDotTunnel(FXItem& fx, ID3D11DeviceContext* context)
