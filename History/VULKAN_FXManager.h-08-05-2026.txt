VULKAN_FXManager.h - 08-05-2026
=================================
Changes for WarpDotTunnel FX system (Vulkan mirror of DX_FXManager.h changes):

1. FXType enum: added WarpDotTunnel value.

2. TunnelSpinCycle enum added (same as DX version):
   - None, Clockwise, AntiClockwise

3. VKTunnelRing struct added: zPos, spinAngle, cx, cy, alive.

4. VKWarpTunnelData struct added:
   - Same fields as WarpTunnelData; static constexpr kMaxXYRadius = 300.0f;
   - std::vector<VKTunnelRing> rings

5. VKFXItem struct: added warpTunnelData member (VKWarpTunnelData).

6. VKActiveFXState struct: added tunnelActive (bool) and tunnelID (int).

7. VKFXManager class:
   - Public: int tunnelID = 0;
   - Public: Init3DWarpDOTTunnel(x, y, z, minRadius, maxRadius, spinCycle,
                                  travelSpeed, reverseTravel, dotsPerCircle, density)
   - Public: StopWarpDotTunnel()
   - Private: UpdateWarpDotTunnel(VKFXItem& fx, float deltaTime)
   - Private: RenderWarpDotTunnel(VKFXItem& fx, VkCommandBuffer cmd)
