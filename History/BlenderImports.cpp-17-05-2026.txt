BlenderImports.cpp — History 17-05-2026
ConvertNodeMatrix made cross-platform; BuildConfig uses GLTF_DEFAULT_FLIP.

Changes:
- BuildConfig: replaced hardcoded FLIP_Z with GLTF_DEFAULT_FLIP (set per-renderer in header).
- ConvertNodeMatrix: DX version uses XMMatrixScaling (wrapped in DX guard);
  OpenGL/Vulkan version performs manual scale-flip via row/column negation on Matrix4x4.m[][]:
  FLIP_X: negate col 0 (m[0][0..3] *= -1)
  FLIP_Y: negate col 1
  FLIP_Z: negate col 2
  FLIP_NONE: return matrix unchanged
- No changes to mesh-data import logic.
